<nav class="navbar fixed-top top-navbar" data-controller="top-navbar" data-top-navbar-target="navbar">
  <div class="container d-flex justify-content-between align-items-center" style="height: 56px;">
    <!-- App Logo -->
    <div class="navbar-logo">
      <%= link_to image_tag("logo_complet.png", alt: "App Logo", class: "app-logo"), root_path %>
    </div>

    <!-- User Picture -->
    <div class="navbar-user">
      <% if current_user %>
      <%= link_to cl_image_tag(current_user.photo.key, alt: "User", class: "user-picture"), account_path %>
      <% end %>
    </div>

    </div>
  </div>
</nav>
